<nav>
    <ul id="nav">
            {% for entity in entities %}
                <li>
                    <a href="{{ path("other", {"translit" : entity.translit }) }}">
                        {{entity.title}}
                    </a>
                </li>
            {% endfor %}
    </ul>
</nav>
